export interface RenderOptions {
  tileWidth: number;
  tileHeight: number;
  gridWidth: number;
  gridHeight: number;
  scale: number;
}

export interface ViewportSize {
  width: number;
  height: number;
}
